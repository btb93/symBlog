{% extends 'base.html.twig' %}
{% block title %}Article{% endblock %}
{% block body %}
    <h1>Article</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>
                    {{ article.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>
                    {{ article.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>
                    {{ article.contenu }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>
                    {{ article.image }}</td>
            </tr>
            <tr>
                <th>CreateAt</th>
                <td>
                    {{ article.createAt ? article.createAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>
    <a class="btn btn-primary" href="{{ path('article_index') }}">Retour à la  liste</a>
    <a class="btn btn-danger" href="{{ path('article_edit', {'id': article.id}) }}">Editer</a>
    <p></p>
    {{ include('article/_delete_form.html.twig') }}
{% endblock %}