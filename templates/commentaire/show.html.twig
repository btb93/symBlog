{% extends 'base.html.twig' %}
{% block title %}Commentaire{% endblock %}
{% block body %}
    <h1>Commentaire</h1>
    <table class="table table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>
                    {{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>
                    {{ commentaire.auteur }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>
                    {{ commentaire.contenu }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>
                    {{ commentaire.createdAt ? commentaire.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>
    <a class="btn btn-primary" href="{{ path('commentaire_index') }}">Retour à la liste</a>
    <a class="btn btn-success" href="{{ path('commentaire_edit', {'id': commentaire.id}) }}">Editer</a>
    <p></p>
    {{ include('commentaire/_delete_form.html.twig') }}
{% endblock %}